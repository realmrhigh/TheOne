package com.high.theone.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// ─── MPC ONE+ Dark Theme Palette ───────────────────────────────────────────
// Device body: deep charcoal with subtle red metallic tint (matching MPC ONE+ Red)
val MpcBodyDark       = Color(0xFF1A1A1A)   // Deep charcoal – main background
val MpcBodyMid        = Color(0xFF242424)   // Mid-level surface
val MpcBodyLight      = Color(0xFF2E2E2E)   // Elevated surface / card bg
val MpcPanelBg        = Color(0xFF1C1C1C)   // Panel / section background
val MpcScreenBg       = Color(0xFF0D0D14)   // Display screen deep background
val MpcScreenFg       = Color(0xFF1A1A2E)   // Secondary screen layer

// Accent: MPC ONE+ signature red
val MpcRed            = Color(0xFFC0392B)   // Primary brand red
val MpcRedBright      = Color(0xFFE74C3C)   // Bright/active red
val MpcRedGlow        = Color(0x55E74C3C)   // Glow overlay

// Display content colors (piano roll / waveform)
val MpcDisplayBlue    = Color(0xFF3498DB)   // Step/bar highlight
val MpcDisplayCyan    = Color(0xFF00BCD4)   // Playhead / cursor
val MpcDisplayYellow  = Color(0xFFFFEB3B)   // Velocity bars
val MpcDisplayGreen   = Color(0xFF4CAF50)   // Active step / playing note
val MpcDisplayOrange  = Color(0xFFFF9800)   // Secondary velocity bar color

// Pad border colours (each pad has its own glow – mirroring the MPC ONE+ rainbow borders)
val MpcPadPink        = Color(0xFFFF4081)   // Pad 13
val MpcPadMagenta     = Color(0xFFE040FB)   // Pad 14
val MpcPadPurple      = Color(0xFF7C4DFF)   // Pad 15
val MpcPadGreen       = Color(0xFF00E676)   // Pad 16
val MpcPadOrange      = Color(0xFFFF6D00)   // Pad 9
val MpcPadYellow      = Color(0xFFFFD740)   // Pad 10
val MpcPadTeal        = Color(0xFF1DE9B6)   // Pad 11
val MpcPadBlue        = Color(0xFF448AFF)   // Pad 12
val MpcPadLightPink   = Color(0xFFFF80AB)   // Pad 5
val MpcPadLavender    = Color(0xFFB388FF)   // Pad 6
val MpcPadSky         = Color(0xFF80D8FF)   // Pad 7
val MpcPadLime        = Color(0xFFCCFF90)   // Pad 8
val MpcPadRed         = Color(0xFFFF1744)   // Pad 1
val MpcPadAmber       = Color(0xFFFFAB40)   // Pad 2
val MpcPadGold        = Color(0xFFFFD600)   // Pad 3
val MpcPadMint        = Color(0xFF69F0AE)   // Pad 4

// Button surface colors
val MpcButtonDark     = Color(0xFF1E1E1E)   // Default button
val MpcButtonMid      = Color(0xFF333333)   // Hover / secondary button
val MpcButtonActive   = Color(0xFF4A4A4A)   // Active/pressed button
val MpcButtonText     = Color(0xFFE0E0E0)   // Button label
val MpcButtonSubtext  = Color(0xFF9E9E9E)   // Secondary label (shift function)

// Q-LINK knob colors
val MpcKnobRing       = Color(0xFF3A3A3A)   // Knob outer ring
val MpcKnobIndicator  = Color(0xFF64B5F6)   // Knob position indicator

// On-screen text
val MpcTextPrimary    = Color(0xFFEEEEEE)
val MpcTextSecondary  = Color(0xFF9E9E9E)
val MpcTextAccent     = Color(0xFF64B5F6)   // Highlighted / selected text